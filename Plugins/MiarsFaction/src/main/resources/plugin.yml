name: MiarsFaction
main: cn.mcarl.miars.faction.MiarsFaction
version: ${project.version}

authors:
  - Red_Carl

api-version: 1.8

depend:
  - MiarsCore

commands:
  "faction":
    description: 派系主指令。
    aliases:
      - f